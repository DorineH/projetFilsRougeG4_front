import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
      navbar: {
        main: '#424242',
      },
      secondary: {
        main: '#880e4f',
      }
    },
    background: {
        default: 'white',
        profil: '#f3e5f5'
    },
    typography: {
        title: {
            // fontSize: 50,
            color: 'black'}
    }
});